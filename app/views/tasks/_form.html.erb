<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :project_id %><br />
    <%= f.number_field :project_id %>
  </div>
  <div class="field">
    <%= f.label :is_done %><br />
    <%= f.check_box :is_done %>
  </div>
  <div class="field">
    <%= f.label :time_end %><br />
    <%= f.datetime_select :time_end %>
  </div>
  <div class="field">
    <%= f.label :prioritise %><br />
    <%= f.number_field :prioritise %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
