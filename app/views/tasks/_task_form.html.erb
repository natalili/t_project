<%= form_for(@task, :remote=>true)  do |f| %>
    <%= f.hidden_field :project_id%>
    <%= f.text_field :name, :size=>"80%" %>
    <%= f.submit f.object.new_record? ? 'Add Task' : 'Update'%>
<% end %>  
